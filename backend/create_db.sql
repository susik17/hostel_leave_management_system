-- Run in PostgreSQL (pgAdmin or Query Tool) if you want a separate database:
-- CREATE DATABASE hostel_leave;
-- Then set DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/hostel_leave

-- Or use existing "project" database - tables will be created in public schema
